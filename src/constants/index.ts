/**
 * Public constants entrypoint (`@roastery/barista/constants`).
 *
 * Re-exports the {@link HTTP_STATUS} map together with its derived
 * {@link HttpStatusName} and {@link HttpStatusCode} types.
 *
 * @module
 */

export {
	HTTP_STATUS,
	type HttpStatusCode,
	type HttpStatusName,
} from "./http-status";
