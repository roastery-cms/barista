/**
 * Public type entrypoint (`@roastery/barista/types`).
 *
 * Re-exports the shared types that describe the shapes flowing through the
 * Barista request/response pipeline.
 *
 * @module
 */

export type { BaristaResponse } from "./barista-response";
