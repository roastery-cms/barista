/**
 * Barrel for the {@link errorHandler} constants.
 *
 * Re-exports {@link STATUS_CODE_MAP}, the table the handler uses to map a
 * `CoreException` to its HTTP status code.
 *
 * @module
 */

export { STATUS_CODE_MAP } from "./status-code-map";
